# Domain documentation

- このリポジトリは単一のdomain contextとして扱う。
- `/ops` をIA、要件、文章規則、デザイン規則のsource of truthとし、判断の履歴は `/ops/adr` に置く。
- domainまたはarchitectureに関わる作業では、関連する `/ops` 文書とADRを先に読む。
- `CONTEXT.md` や追加のglossaryは、必要性が明確になった時点で追加する。
- 既存のcanonical termとADRに矛盾する変更は、黙って上書きせずオーナーへ確認する。
