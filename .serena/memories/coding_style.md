Content markdown requires `slug`, `title`, and <=5 tags; structure is H1 once per page, H2/H3 hierarchy, with JA/EN parity. CSS sticks to vanilla + nesting, referencing tokens from `/ops/style-design.md` (dark palette, fixed spacing scale, 3-column layout). JS is always ESM modules with import maps, no frameworks or bundlers. SVG icons live under `src/assets/icons` with `viewBox`+`currentColor`. Workflows render copy/download controls per `/ops/requirements.md`, Gyazo images normalize to cached variants with width/height metadata, and hero/tag behavior follows the mock + ADRs.